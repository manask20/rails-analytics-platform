# My Project
Auto-Scaling Data Job Platform (Serverless + Queue + Workers)

Core idea

Users upload a dataset + submit a job → system queues work → workers process → progress + logs shown in a simple dashboard → retries + idempotency handled → optional AWS deployment.
